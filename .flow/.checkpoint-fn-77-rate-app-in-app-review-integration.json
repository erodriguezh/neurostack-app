{
  "created_at": "2026-03-17T07:15:35.138739Z",
  "epic": {
    "data": {
      "branch_name": "fn-77-rate-app-in-app-review-integration",
      "completion_review_status": "unknown",
      "completion_reviewed_at": null,
      "created_at": "2026-03-16T20:00:58.511721Z",
      "default_impl": null,
      "default_review": null,
      "default_sync": null,
      "depends_on_epics": [
        "fn-73-settings-screen-implementation",
        "fn-76-integrate-userorient-flutter-for"
      ],
      "id": "fn-77-rate-app-in-app-review-integration",
      "next_task": 1,
      "plan_review_status": "ship",
      "plan_reviewed_at": "2026-03-16T20:25:25.884195Z",
      "spec_path": ".flow/specs/fn-77-rate-app-in-app-review-integration.md",
      "status": "open",
      "title": "Rate App / In-App Review Integration",
      "updated_at": "2026-03-16T20:25:25.884368Z"
    },
    "spec": "# Rate App / In-App Review Integration\n\n## Overview\n\nAdd in-app review functionality using the `in_app_review` Flutter package. Two flows:\n\n1. **Programmatic** \u2014 silently call `requestReview()` after the user logs their 5th, 15th, or 35th session (counted from synced + pending local sessions). Triggered ~2 seconds after the log session modal closes (i.e., after `await showLogSessionModal()` returns, NOT inside `onSessionLogged` which fires pre-dismiss).\n2. **Manual** \u2014 Settings \"Rate the App\" tile navigates to a dedicated Rate App screen with two CTAs: \"Rate on App Store\" (`openStoreListing()`) and \"Quick Rating\" (`requestReviewForScreen()`).\n\n## Architecture\n\n### InAppReviewService\n\n- Location: `lib/core/utils/in_app_review/in_app_review_service.dart`\n- Pattern: follows `UserOrientService` (`lib/core/utils/userorient/userorient_service.dart`)\n  - Plain class, `Logger` instance\n  - `_isInitialized` boolean guard \u2014 all public methods no-op when false\n  - `init()` with platform guard (`kIsWeb` \u2192 return, non-iOS/Android \u2192 return)\n  - Lazy singleton in `locator_config.dart`\n  - `init()` called in `StartupViewModel.initializeApp()` wrapped in try/catch\n\n### Test seam\n\nThe static `InAppReview.instance` singleton is not directly mockable. Introduce a thin adapter interface:\n\n```dart\nabstract class InAppReviewAdapter {\n  Future<bool> isAvailable();\n  Future<void> requestReview();\n  Future<void> openStoreListing({String? appStoreId});\n}\n```\n\nDefault implementation delegates to `InAppReview.instance`. `InAppReviewService` takes this adapter as a constructor dependency, enabling full unit test coverage via mock adapter.\n\n**Note on Android**: The `in_app_review` package auto-detects the Android package name from the manifest \u2014 no explicit parameter needed for `openStoreListing()` on Android. The `appStoreId` parameter is iOS-only. `PLAY_STORE_PACKAGE_NAME` env config is used exclusively by `RateAppViewModel` for the `url_launcher` fallback URL construction on unsupported platforms.\n\n### Platform detection\n\n`kIsWeb` is compile-time and cannot be swapped in VM tests. Abstract platform detection behind an injectable helper or use `defaultTargetPlatform` (which IS swappable via `debugDefaultTargetPlatformOverride` in tests) for the iOS/Android guard. The `kIsWeb` guard is a compile-time early return that does not need a dedicated unit test \u2014 test non-mobile guard via `debugDefaultTargetPlatformOverride`.\n\n### Constructor dependencies\n\n```dart\nInAppReviewService({\n  required SharedPreferences prefs,\n  required NotifyService notifyService,\n  required InAppReviewAdapter adapter,\n})\n```\n\n- `SharedPreferences` \u2014 review-request index persistence\n- `NotifyService` \u2014 error toasts when `openStoreListing()` fails\n- `InAppReviewAdapter` \u2014 test seam for the `in_app_review` plugin\n\n### Key methods\n\n- `init()` \u2014 guards on platform + reads env config via `const String.fromEnvironment('APP_STORE_ID')` and `const String.fromEnvironment('PLAY_STORE_PACKAGE_NAME')` (matching existing codebase pattern, NOT `AppEnvironment`)\n- `requestReviewIfNeeded(int sessionCount, String userId)` \u2014 programmatic flow: checks session count against thresholds `[5, 15, 35]`, reads review-request index from SharedPreferences, calls `adapter.requestReview()` if threshold crossed, increments index. All wrapped in try/catch. Silent on failure.\n- `requestReviewForScreen()` \u2014 manual flow (Rate App screen): try `adapter.requestReview()`, on failure auto-fallback to `openStoreListing()`.\n- `openStoreListing()` \u2014 calls `adapter.openStoreListing(appStoreId: ...)`. On failure, shows error toast via `NotifyService`.\n\n### Session count source\n\n- Count captured **immediately after session save** (before the 2-second delay) to avoid sync-window race conditions where a session might exist in both pending and synced stores\n- Capture: `final count = getSyncedSessions(userId).length + getPendingSessions(userId).length;`\n- Both calls are `Future<List<...>>` \u2014 must be awaited (use `Future.wait` for parallel fetch)\n- Then `await Future.delayed(Duration(seconds: 2))` before calling `requestReviewIfNeeded(count, userId)`\n- This avoids the double-count risk: count is a snapshot taken before sync can move the session between stores\n\n### Review-request index persistence\n\n- SharedPreferences key: `'review_request_index_<userId>'`\n- Value: int (0, 1, 2, or 3) \u2014 index into `[5, 15, 35]` thresholds\n- 0 = no prompts shown yet, 3 = all thresholds exhausted\n- Follows `TrialReminderService` key pattern (`lib/paywall/data/trial_reminder_service.dart`)\n- Default 0 on fresh install / missing key\n- Survives logout (intentional \u2014 same user, same history)\n\n### Programmatic trigger flow\n\n1. User logs session \u2192 `onSessionLogged` callback fires (pre-dismiss) \u2192 modal closes via `Navigator.pop()`\n2. `await showLogSessionModal()` returns (modal fully dismissed)\n3. **Immediately** capture session count snapshot: `await Future.wait([getSyncedSessions(userId), getPendingSessions(userId)])` \u2014 sum lengths\n4. `await Future.delayed(Duration(seconds: 2))` (so success toast is visible first)\n5. Call `InAppReviewService.requestReviewIfNeeded(count, userId)`\n6. Service checks threshold, fires `requestReview()` if eligible, increments index\n7. All wrapped in try/catch \u2014 non-critical, app works without it\n\n### Manual flow (Rate App screen)\n\n- Route: `/settings/rate-app` \u2014 flat `RouteEntry(path: '/settings/rate-app', ...)` mirroring `/settings/contact` pattern in `route_config.dart`. NOT a nested child route. `requiresAuth: true`, no bottom nav.\n- Close button (top right) \u2192 `Navigator.pop()` back to Settings\n- Neurostack logo (same asset as main menu) centered in upper area\n- Title: \"Enjoying Neurostack?\"\n- Body: \"Your feedback helps improve the app and reach more people who can benefit from evidence-based wellness protocols.\"\n- Primary CTA: \"Rate on App Store\" \u2192 `openStoreListing()`\n- Secondary CTA: \"Quick Rating\" \u2192 `requestReviewForScreen()` (try requestReview, fallback to openStoreListing)\n- Both buttons always visible (even after 3 thresholds exhausted \u2014 simpler)\n- \"Quick Rating\" does NOT count against the programmatic threshold tracker (independent flows)\n- Button styling: `AppPrimaryCta` may need icon configurability (currently hardcodes arrow). If not feasible, use a custom styled button matching the design system for this screen only.\n\n### Unsupported platform behavior (web/desktop)\n\nThe \"Rate the App\" tile is visible on all platforms. On unsupported platforms where `_isInitialized` is false:\n- Rate App screen still navigable (tile always visible)\n- \"Rate on App Store\" \u2192 **`RateAppViewModel` owns this fallback**: constructs and launches a public store URL via `url_launcher` (already used in `settings_view_model.dart`). URL: `https://apps.apple.com/app/id<APP_STORE_ID>` (iOS) or `https://play.google.com/store/apps/details?id=<PACKAGE_NAME>` (Android/web). The ViewModel checks `isServiceInitialized` and branches accordingly.\n- \"Quick Rating\" button \u2192 hidden or disabled with explanatory text when service is not initialized (since `requestReview()` is impossible on web)\n- `InAppReviewService` stays platform-agnostic \u2014 no `url_launcher` dependency. Fallback lives in `RateAppViewModel`.\n- This prevents dead-end UX where buttons do nothing\n\n### Env config\n\n- `APP_STORE_ID` \u2014 Apple App Store numeric ID, read via `const String.fromEnvironment('APP_STORE_ID')` (matching existing codebase pattern). Used by `InAppReviewService.openStoreListing()` on iOS and by `RateAppViewModel` for fallback URL.\n- `PLAY_STORE_PACKAGE_NAME` \u2014 Android package name, read via `const String.fromEnvironment('PLAY_STORE_PACKAGE_NAME')`. Used only by `RateAppViewModel` for `url_launcher` fallback URL construction. The `in_app_review` plugin auto-detects the Android package name from the manifest.\n- Added to `env/default.env.json` with placeholder values\n- **Platform-specific config validation**: `init()` only requires the config relevant to the current platform. On iOS, `APP_STORE_ID` must be present. On Android, no env config is required (package name is auto-detected). Missing iOS config on iOS \u2192 `_isInitialized = false`. Missing Android config on Android \u2192 still initializes (plugin handles it). `PLAY_STORE_PACKAGE_NAME` is never required for service initialization \u2014 it's only used by the ViewModel fallback.\n\n### Error handling\n\n- `requestReview()` throws \u2192 auto-fallback to `openStoreListing()` (on Rate App screen via `requestReviewForScreen()`) or silent swallow (programmatic via `requestReviewIfNeeded()`)\n- `openStoreListing()` throws \u2192 error toast via `NotifyService`\n- `isAvailable()` checked before `requestReview()` \u2014 if false, fallback to `openStoreListing()` on screen, skip on programmatic\n- iOS 18 freeze (#147) and Android NPE (#175) mitigated by try/catch\n- Missing platform-specific env config (e.g., `APP_STORE_ID` on iOS) \u2192 service stays uninitialized on that platform, all methods no-op\n\n### Analytics\n\n> **Note**: Analytics events (e.g., `review_requested`, `store_listing_opened`) are deferred to a future analytics epic. Leave a `// TODO(analytics): track review prompt event` comment at trigger points.\n\n## Dependencies\n\n- `in_app_review` Flutter package (add to `pubspec.yaml`)\n- Completed: fn-76 (UserOrient integration \u2014 pattern to follow)\n- Completed: fn-73 (Settings screen \u2014 tile placeholder exists)\n\n## Quick commands\n\n```bash\n# Verify package added\ngrep 'in_app_review' pubspec.yaml\n\n# Run tests\nflutter test test/core/utils/in_app_review/\nflutter test test/settings/\n\n# Analyze\nflutter analyze\n```\n\n## Acceptance\n\n- [ ] `in_app_review` package added to `pubspec.yaml`\n- [ ] `InAppReviewAdapter` interface + default implementation created (test seam)\n- [ ] `InAppReviewService` follows `UserOrientService` pattern (lazy singleton, platform guard via `defaultTargetPlatform`, init guard, try/catch init in startup)\n- [ ] Constructor injects `SharedPreferences`, `NotifyService`, and `InAppReviewAdapter`\n- [ ] Programmatic `requestReviewIfNeeded()` fires after 5th, 15th, 35th session (synced + pending count)\n- [ ] Session count snapshot captured immediately after modal dismiss, before 2-second delay\n- [ ] ~2-second delay before programmatic trigger fires\n- [ ] Review-request index persisted in SharedPreferences (user-scoped key)\n- [ ] Rate App screen accessible from Settings \"Rate the App\" tile\n- [ ] Rate App screen has: logo, title, body text, \"Rate on App Store\" primary CTA, \"Quick Rating\" secondary CTA, close button\n- [ ] \"Rate on App Store\" calls `openStoreListing()` with App Store ID from env config\n- [ ] `openStoreListing()` passes `appStoreId` on iOS; Android auto-detected by plugin\n- [ ] \"Quick Rating\" calls `requestReviewForScreen()` with auto-fallback to `openStoreListing()` on failure\n- [ ] Route `/settings/rate-app` added as flat `RouteEntry` in `route_config.dart` (requiresAuth, no bottom nav)\n- [ ] Env config read via `const String.fromEnvironment(...)` (not AppEnvironment)\n- [ ] `env/default.env.json` updated with `APP_STORE_ID` and `PLAY_STORE_PACKAGE_NAME`\n- [ ] Platform-specific config validation: iOS requires `APP_STORE_ID`, Android needs nothing from env for service init\n- [ ] Missing platform config \u2192 service stays uninitialized on that platform, graceful degradation\n- [ ] Web/unsupported platforms: Rate App screen uses `url_launcher` fallback for store link, \"Quick Rating\" hidden/disabled\n- [ ] `RateAppViewModel` fallback URL construction tested (both iOS and Android/web URLs)\n- [ ] Programmatic trigger sequencing tested: count captured before delay, then trigger fires after delay\n- [ ] Screen prompt doc created at `docs/best_practices/design/screen-prompts/12-rate-app-screen.md`\n- [ ] Settings screen spec and screen prompt updated (placeholder notes removed)\n- [ ] `docs/README.md` updated with new links\n- [ ] Screen functional specs updated with Rate App entry and metadata counts corrected\n- [ ] All try/catch wrapped \u2014 feature is non-critical\n- [ ] Unit tests for `InAppReviewService` threshold logic via mock `InAppReviewAdapter`\n- [ ] `flutter analyze` passes\n"
  },
  "epic_id": "fn-77-rate-app-in-app-review-integration",
  "schema_version": 2,
  "tasks": [
    {
      "data": {
        "assignee": null,
        "claim_note": "",
        "claimed_at": null,
        "created_at": "2026-03-16T20:02:14.251573Z",
        "depends_on": [],
        "epic": "fn-77-rate-app-in-app-review-integration",
        "id": "fn-77-rate-app-in-app-review-integration.1",
        "priority": null,
        "spec_path": ".flow/tasks/fn-77-rate-app-in-app-review-integration.1.md",
        "status": "todo",
        "title": "Add in_app_review package, InAppReviewService, and env config",
        "updated_at": "2026-03-16T20:13:28.035693Z"
      },
      "id": "fn-77-rate-app-in-app-review-integration.1",
      "runtime": null,
      "spec": "# fn-77-rate-app-in-app-review-integration.1 Add in_app_review package, InAppReviewService, and env config\n\n## Description\nAdd the `in_app_review` package, create `InAppReviewAdapter` interface (test seam), create `InAppReviewService` following the `UserOrientService` pattern, update env config, register in service locator, and wire startup init.\n\n**Size:** M\n**Files:**\n- `pubspec.yaml` (add `in_app_review` dependency)\n- `lib/core/utils/in_app_review/in_app_review_adapter.dart` (new \u2014 interface + default impl)\n- `lib/core/utils/in_app_review/in_app_review_service.dart` (new)\n- `lib/config/locator_config.dart` (register lazy singleton)\n- `lib/startup/startup_view_model.dart` (add init call)\n- `env/default.env.json` (add `APP_STORE_ID`, `PLAY_STORE_PACKAGE_NAME`)\n- `test/core/utils/in_app_review/in_app_review_service_test.dart` (new)\n- `test/mocks/mock_services.dart` (add `MockInAppReviewService`, `MockInAppReviewAdapter`)\n\n## Approach\n\n- **Test seam**: Create `InAppReviewAdapter` abstract class with `isAvailable()`, `requestReview()`, `openStoreListing({String? appStoreId})`. Default implementation `DefaultInAppReviewAdapter` delegates to `InAppReview.instance`. This enables full mocking in unit tests.\n- Follow `UserOrientService` pattern at `lib/core/utils/userorient/userorient_service.dart`\n- Plain class with `Logger`, `_isInitialized` guard\n- **Platform guard**: Use `defaultTargetPlatform` (swappable in tests via `debugDefaultTargetPlatformOverride`) for iOS/Android check. `kIsWeb` for compile-time web guard (no unit test needed for this \u2014 it's compile-time).\n- **Constructor dependencies**: `SharedPreferences` (review-request index), `NotifyService` (error toasts), `InAppReviewAdapter` (test seam)\n- **Env config**: Read via `const String.fromEnvironment('APP_STORE_ID')` and `const String.fromEnvironment('PLAY_STORE_PACKAGE_NAME')` \u2014 matching existing codebase pattern, NOT `AppEnvironment`\n- **Platform-specific config validation**: On iOS, require `APP_STORE_ID` \u2014 if empty, log warning and keep `_isInitialized = false`. On Android, no env config needed for service init (plugin auto-detects package name from manifest). `PLAY_STORE_PACKAGE_NAME` is only used by `RateAppViewModel` for `url_launcher` fallback, not by the service.\n- SharedPreferences key: `'review_request_index_<userId>'`, default 0\n- Thresholds: `[5, 15, 35]` \u2014 index into this list\n- **Three public methods**:\n  - `requestReviewIfNeeded(int sessionCount, String userId)` \u2014 programmatic: check threshold, call `adapter.requestReview()`, increment index. Silent on failure.\n  - `requestReviewForScreen()` \u2014 manual: try `adapter.requestReview()`, on failure fallback to `openStoreListing()`\n  - `openStoreListing()` \u2014 pass `appStoreId` from env config. On failure, show error toast via `NotifyService`\n- Register as `Module<InAppReviewService>(builder: ..., lazy: true)` in `locator_config.dart` near `UserOrientService` registration (~line 186)\n- Init in `startup_view_model.dart` after UserOrient init (~line 89), wrapped in try/catch\n- `// TODO(analytics): track review prompt event` at trigger points\n\n## Key context\n\n- `isAvailable()` must be called before `requestReview()` \u2014 necessary but not sufficient\n- iOS 18 freeze issue (#147): `requestReview()` can block main thread ~8s \u2014 try/catch mitigates\n- Android NullPointerException (#175): wrap in try/catch\n- `openStoreListing()` passes `appStoreId` on iOS (from env config); Android package name auto-detected by plugin (no env config needed). `PLAY_STORE_PACKAGE_NAME` is only used by `RateAppViewModel` for `url_launcher` fallback URLs.\n- `requestReview()` gives zero feedback \u2014 no way to know if dialog was shown\n\n## Unit tests\n\n- Test threshold logic via mock `InAppReviewAdapter`: session count 4 \u2192 no trigger, 5 \u2192 trigger, 6 \u2192 no trigger (already fired), 15 \u2192 trigger, 35 \u2192 trigger, 36 \u2192 no trigger (exhausted)\n- Test `_isInitialized` guard: methods are no-ops when not initialized\n- Test SharedPreferences index persistence: increments correctly, survives across calls\n- Test non-mobile platform guard via `debugDefaultTargetPlatformOverride` (set to linux \u2192 init skips)\n- Test `requestReviewForScreen()` fallback: mock adapter throws \u2192 verify `openStoreListing()` called\n- Test `openStoreListing()` failure \u2192 verify `NotifyService` toast fired\n- Test missing platform config (e.g., missing `APP_STORE_ID` on iOS) \u2192 `_isInitialized` stays false\n- Test Android init succeeds without `APP_STORE_ID` (not required on Android)\n\n## Acceptance\n- [ ] `in_app_review` package added to `pubspec.yaml` and `flutter pub get` succeeds\n- [ ] `InAppReviewAdapter` abstract class + `DefaultInAppReviewAdapter` created (test seam)\n- [ ] `InAppReviewService` created at `lib/core/utils/in_app_review/in_app_review_service.dart`\n- [ ] Constructor injects `SharedPreferences`, `NotifyService`, and `InAppReviewAdapter`\n- [ ] Platform guard uses `defaultTargetPlatform` (testable), `kIsWeb` for compile-time web check\n- [ ] `requestReviewIfNeeded(int sessionCount, String userId)` checks thresholds `[5, 15, 35]`\n- [ ] `requestReviewForScreen()` tries `requestReview()`, fallback to `openStoreListing()` on failure\n- [ ] `openStoreListing()` passes `appStoreId` on iOS; Android auto-detected by plugin (no param needed)\n- [ ] Error toast shown via `NotifyService` when `openStoreListing()` fails\n- [ ] Env config read via `const String.fromEnvironment(...)` \u2014 NOT `AppEnvironment`\n- [ ] Platform-specific config: iOS requires `APP_STORE_ID`, Android needs nothing for service init\n- [ ] Missing platform config \u2192 `_isInitialized = false` on that platform, graceful degradation\n- [ ] Review-request index persisted in SharedPreferences with user-scoped key\n- [ ] `env/default.env.json` updated with `APP_STORE_ID` and `PLAY_STORE_PACKAGE_NAME` placeholder keys\n- [ ] Registered as lazy singleton in `locator_config.dart`\n- [ ] `init()` called in `startup_view_model.dart` with try/catch\n- [ ] `MockInAppReviewService` and `MockInAppReviewAdapter` added to `test/mocks/mock_services.dart`\n- [ ] Unit tests cover: threshold logic, index persistence, platform guard (via debugDefaultTargetPlatformOverride), init guard, requestReviewForScreen fallback, openStoreListing toast on failure, missing env config\n- [ ] `// TODO(analytics)` comments at service-level trigger points (programmatic trigger call sites are in fn-77.3)\n- [ ] `flutter analyze` passes\n\n## Done summary\nTBD\n\n## Evidence\n- Commits:\n- Tests:\n- PRs:\n"
    },
    {
      "data": {
        "assignee": null,
        "claim_note": "",
        "claimed_at": null,
        "created_at": "2026-03-16T20:02:14.383907Z",
        "depends_on": [
          "fn-77-rate-app-in-app-review-integration.1"
        ],
        "epic": "fn-77-rate-app-in-app-review-integration",
        "id": "fn-77-rate-app-in-app-review-integration.2",
        "priority": null,
        "spec_path": ".flow/tasks/fn-77-rate-app-in-app-review-integration.2.md",
        "status": "todo",
        "title": "Rate App screen, route, and ViewModel",
        "updated_at": "2026-03-16T20:13:49.115856Z"
      },
      "id": "fn-77-rate-app-in-app-review-integration.2",
      "runtime": null,
      "spec": "# fn-77-rate-app-in-app-review-integration.2 Rate App screen, route, and ViewModel\n\n## Description\nCreate the Rate App screen (view + view model), add the route to GoRouter config, and style it following existing screen patterns. Handle unsupported platform behavior.\n\n**Size:** M\n**Files:**\n- `lib/settings/views/rate_app_view.dart` (new)\n- `lib/settings/view_models/rate_app_view_model.dart` (new)\n- `lib/config/route_config.dart` (add `/settings/rate-app` route)\n- `lib/core/ui/constants/widget_keys.dart` (add Rate App keys)\n- `test/settings/view_models/rate_app_view_model_test.dart` (new)\n\n## Approach\n\n- **Route**: Add `/settings/rate-app` as a flat `RouteEntry(path: '/settings/rate-app', ...)` mirroring the `/settings/contact` pattern in `route_config.dart`. NOT a nested child route. `requiresAuth: true`, no bottom nav shell.\n- **RateAppView**: StatelessWidget or StatefulWidget depending on state needs.\n  - Layout: `Scaffold` with `AppBar` (close button top-right, no title), body is a centered column\n  - Neurostack logo: same asset used in the main menu / splash (find via existing usage)\n  - Title: \"Enjoying Neurostack?\" \u2014 use `Theme.of(context).textTheme` heading style\n  - Body text: \"Your feedback helps improve the app and reach more people who can benefit from evidence-based wellness protocols.\"\n  - Primary CTA: \"Rate on App Store\" with appropriate icon \u2014 calls `viewModel.openStoreListing()`\n  - Secondary CTA: \"Quick Rating\" with star icon \u2014 calls `viewModel.requestReviewForScreen()`\n  - Button styling: Check if `AppPrimaryCta` supports custom icons. If it hardcodes an arrow icon, use a custom styled button matching the design system for this screen only.\n  - Use `AppSemanticColors` for all color tokens \u2014 no raw `kitColors` references\n  - Use `AppSpacing` constants for all spacing\n- **Unsupported platform behavior** (web/desktop):\n  - \"Rate on App Store\" \u2192 `RateAppViewModel` constructs and launches a public store URL via `url_launcher` (already used in `settings_view_model.dart`): `https://apps.apple.com/app/id<APP_STORE_ID>` or `https://play.google.com/store/apps/details?id=<PACKAGE_NAME>`\n  - \"Quick Rating\" button \u2192 hidden or disabled with explanatory text when `InAppReviewService` is not initialized\n  - This prevents dead-end UX where buttons do nothing\n- **RateAppViewModel**: follows existing ViewModel pattern (e.g., `SettingsViewModel`)\n  - Injects `InAppReviewService`\n  - `openStoreListing()` \u2192 delegates to service. On unsupported platforms, falls back to `url_launcher` with constructed store URL (ViewModel owns this fallback logic since it needs `launchUrl`)\n  - `requestReviewForScreen()` \u2192 delegates to service (try requestReview, fallback to openStoreListing)\n  - Exposes `isServiceInitialized` for the view to conditionally show/hide \"Quick Rating\"\n- **Widget keys**: Add keys to `lib/core/ui/constants/widget_keys.dart` for Rate App screen elements\n\n## Key context\n\n- The close button should use `Navigator.pop()` \u2014 this is a pushed route, back goes to Settings\n- Both buttons always visible on supported platforms regardless of threshold state (decision: simpler UX)\n- \"Quick Rating\" is independent from the programmatic threshold tracker\n- `requestReview()` may silently do nothing \u2014 that's expected, no error to show\n- `openStoreListing()` failure shows error toast (handled by service, not ViewModel)\n- `url_launcher` fallback for unsupported platforms lives in `RateAppViewModel` \u2014 service stays platform-agnostic\n\n## Acceptance\n- [ ] `RateAppView` created at `lib/settings/views/rate_app_view.dart`\n- [ ] `RateAppViewModel` created at `lib/settings/view_models/rate_app_view_model.dart`\n- [ ] Route `/settings/rate-app` added as flat `RouteEntry` in `route_config.dart` (mirrors `/settings/contact` pattern)\n- [ ] Route has `requiresAuth: true`, no bottom nav\n- [ ] Screen has: Neurostack logo (same asset as main menu), \"Enjoying Neurostack?\" title, body text, primary + secondary CTAs, close button\n- [ ] Primary CTA (\"Rate on App Store\") calls `openStoreListing()`\n- [ ] Secondary CTA (\"Quick Rating\") calls `requestReviewForScreen()` with auto-fallback\n- [ ] Unsupported platforms: \"Quick Rating\" hidden/disabled, \"Rate on App Store\" uses `url_launcher` fallback (owned by ViewModel)\n- [ ] Uses `AppSemanticColors`, `AppSpacing` \u2014 no raw color values\n- [ ] Widget keys added to `widget_keys.dart`\n- [ ] ViewModel unit tests cover both button actions and isServiceInitialized state\n- [ ] Fallback URL construction tested: verify correct iOS App Store URL and Android/web Play Store URL are constructed and launched\n- [ ] `flutter analyze` passes\n\n## Done summary\nTBD\n\n## Evidence\n- Commits:\n- Tests:\n- PRs:\n"
    },
    {
      "data": {
        "assignee": null,
        "claim_note": "",
        "claimed_at": null,
        "created_at": "2026-03-16T20:02:14.520109Z",
        "depends_on": [
          "fn-77-rate-app-in-app-review-integration.1",
          "fn-77-rate-app-in-app-review-integration.2"
        ],
        "epic": "fn-77-rate-app-in-app-review-integration",
        "id": "fn-77-rate-app-in-app-review-integration.3",
        "priority": null,
        "spec_path": ".flow/tasks/fn-77-rate-app-in-app-review-integration.3.md",
        "status": "todo",
        "title": "Wire Settings tile and programmatic session trigger",
        "updated_at": "2026-03-16T20:14:10.222937Z"
      },
      "id": "fn-77-rate-app-in-app-review-integration.3",
      "runtime": null,
      "spec": "# fn-77-rate-app-in-app-review-integration.3 Wire Settings tile and programmatic session trigger\n\n## Description\nWire the Settings \"Rate the App\" tile to navigate to the Rate App screen via `SettingsViewModel`, and add the programmatic `requestReview()` trigger after session logging in HomeView and ProgressView.\n\n**Size:** M\n**Files:**\n- `lib/settings/settings_view.dart` (wire tile tap to ViewModel method)\n- `lib/settings/settings_view_model.dart` (add `goToRateApp()` method)\n- `lib/settings/widgets/settings_support_section.dart` (update trailing icon)\n- `lib/home/home_view.dart` (add programmatic trigger after `await showLogSessionModal()`)\n- `lib/progress/progress_view.dart` (add programmatic trigger after `await showLogSessionModal()`)\n\n## Approach\n\n### Settings tile wiring\n- Add `goToRateApp()` method to `SettingsViewModel` \u2014 following existing pattern of `goToPaywall()`, `goToContact()`, etc. Navigation goes through the ViewModel, not the view directly.\n- In `settings_view.dart:109`, replace `onRateAppTap: () {}` with `onRateAppTap: _viewModel.goToRateApp` (or equivalent delegate pattern matching existing tiles)\n- In `settings_support_section.dart`, change the Rate App tile's trailing icon from `LucideIcons.externalLink` to `LucideIcons.chevronRight` (consistent with other in-app navigation tiles like Contact Us / Feature Request)\n\n### Programmatic trigger\n- In `home_view.dart`, after `await showLogSessionModal()` returns (~line 309-313):\n  1. **Immediately** capture session count snapshot (BEFORE the delay to avoid sync-window race):\n     ```\n     final results = await Future.wait([\n       locator<SessionLocalDataSource>().getSyncedSessions(userId),\n       locator<SessionLocalDataSource>().getPendingSessions(userId),\n     ]);\n     final sessionCount = results[0].length + results[1].length;\n     ```\n  2. `await Future.delayed(Duration(seconds: 2))` (so success toast is visible first)\n  3. Call `locator<InAppReviewService>().requestReviewIfNeeded(sessionCount, userId)`\n  4. Wrap entire block in try/catch \u2014 non-critical\n  5. Add `// TODO(analytics): track review prompt event`\n\n- Same pattern in `progress_view.dart` (~line 300-306)\n\n### Test strategy\n- Extract the post-modal trigger logic into a testable helper method (e.g., `_checkAndTriggerReview(String userId)`) to enable unit/widget testing of the snapshot-delay-trigger sequence\n- Add widget test or ViewModel test for Settings tile routing via `goToRateApp()`\n- Verify `goToRateApp()` navigates to `/settings/rate-app`\n\n### Key detail: getting userId\n- In HomeView, userId is available via `_viewModel` state (check how existing code accesses it, e.g., `state.user?.id`)\n- Follow existing patterns for accessing user identity at the view level\n\n## Key context\n\n- The `onSessionLogged` callback fires BEFORE `Navigator.pop()` \u2014 do NOT put the trigger there. Use the post-`await` point where the modal is fully dismissed.\n- `showLogSessionModal` is async and the caller awaits it \u2014 the code after `await` runs when the modal is gone.\n- The 2-second delay is intentional: lets the success toast and haptic feedback land before any review dialog appears.\n- `requestReviewIfNeeded` is fully self-contained (checks threshold, checks index, fires if eligible) \u2014 the caller just passes session count and userId.\n- Both `getSyncedSessions()` and `getPendingSessions()` return `Future<List<...>>` \u2014 use `Future.wait` for parallel fetch.\n- Session count snapshot taken BEFORE delay prevents double-counting during sync handoff.\n- Navigation pattern: SettingsView delegates to SettingsViewModel for all navigation (goToPaywall, goToContact, etc.)\n\n## Acceptance\n- [ ] `goToRateApp()` method added to `SettingsViewModel` (follows existing navigation pattern)\n- [ ] Settings \"Rate the App\" tile delegates to `SettingsViewModel.goToRateApp()`\n- [ ] Tile trailing icon changed from `externalLink` to `chevronRight`\n- [ ] Programmatic trigger added in `home_view.dart` after `await showLogSessionModal()`\n- [ ] Programmatic trigger added in `progress_view.dart` after `await showLogSessionModal()`\n- [ ] Session count snapshot captured immediately (via `Future.wait`), BEFORE the 2-second delay\n- [ ] Both `getSyncedSessions()` and `getPendingSessions()` properly awaited\n- [ ] ~2-second delay after count capture, before `requestReviewIfNeeded` call\n- [ ] Trigger logic extracted into testable helper method\n- [ ] Trigger sequencing tested: count captured before delay, then trigger fires after delay (verify order)\n- [ ] All trigger code wrapped in try/catch\n- [ ] `// TODO(analytics)` comment at trigger call sites\n- [ ] `flutter analyze` passes\n\n## Done summary\nTBD\n\n## Evidence\n- Commits:\n- Tests:\n- PRs:\n"
    },
    {
      "data": {
        "assignee": null,
        "claim_note": "",
        "claimed_at": null,
        "created_at": "2026-03-16T20:02:14.667022Z",
        "depends_on": [
          "fn-77-rate-app-in-app-review-integration.3"
        ],
        "epic": "fn-77-rate-app-in-app-review-integration",
        "id": "fn-77-rate-app-in-app-review-integration.4",
        "priority": null,
        "spec_path": ".flow/tasks/fn-77-rate-app-in-app-review-integration.4.md",
        "status": "todo",
        "title": "Documentation: screen prompt, spec updates, README",
        "updated_at": "2026-03-16T20:14:32.416979Z"
      },
      "id": "fn-77-rate-app-in-app-review-integration.4",
      "runtime": null,
      "spec": "# fn-77-rate-app-in-app-review-integration.4 Documentation: screen prompt, spec updates, README\n\n## Description\nCreate the Rate App screen prompt doc, update Settings screen spec and screen prompt to remove placeholder notes, update README with new links, rewrite the existing rate app spec to proper format, and fix screen functional specs metadata.\n\n**Size:** M\n**Files:**\n- `docs/best_practices/design/screen-prompts/12-rate-app-screen.md` (new \u2014 screen prompt)\n- `docs/best_practices/design/screen-prompts/11-settings-screen.md` (update placeholder note)\n- `docs/specs/20260227120000_spec_settings_screen.md` (update placeholder, out-of-scope, deps)\n- `docs/specs/202603162012_spec_rate_app_integration.md` (rewrite from research notes to proper spec)\n- `docs/best_practices/design/screen-functional-specifications.md` (add Rate App screen entry + fix metadata counts)\n- `docs/README.md` (add screen prompt link + spec link)\n\n## Approach\n\n### New screen prompt: `12-rate-app-screen.md`\n- Follow format of `11-settings-screen.md`\n- Layout: `Scaffold`, `AppBar` with close button (top-right), centered column body\n- Neurostack logo (same asset as main menu) centered in upper area\n- Title: \"Enjoying Neurostack?\" \u2014 heading style\n- Body text: \"Your feedback helps improve the app and reach more people who can benefit from evidence-based wellness protocols.\"\n- Primary CTA: \"Rate on App Store\" (star + external-link icon) \u2014 `openStoreListing()`\n- Secondary CTA: \"Quick Rating\" (star icon) \u2014 `requestReviewForScreen()` with auto-fallback\n- Semantic color tokens only (`AppSemanticColors`), `AppSpacing` grid\n- Route: `/settings/rate-app`, pushed, `requiresAuth: true`, no bottom nav\n- Note unsupported platform behavior (Quick Rating hidden, store link via url_launcher)\n\n### Update `11-settings-screen.md`\n- Change Rate the App tile row: trailing icon `external-link` \u2192 `chevron-right`, action text from placeholder to \"Navigate to /settings/rate-app\"\n\n### Update Settings spec (`20260227120000`)\n- Tile Definitions table: update Rate the App action to \"Navigate to /settings/rate-app\"\n- Remove \"App Store rating integration\" from Out of Scope section\n- Add `in_app_review` to Dependencies\n- Update note block under Tile Definitions (remove placeholder language)\n\n### Rewrite rate app spec (`202603162012`)\n- Transform from best-practices research essay into standard spec format\n- Follow structure of `docs/specs/20260315140000_spec_userorient_integration.md`\n- Include: Goal & Context, Architecture (InAppReviewService, InAppReviewAdapter, constructor deps), API Contracts (method signatures), Edge Cases (iOS 18 freeze, Android NPE, missing config, unsupported platforms), Acceptance Criteria, Boundaries\n- Keep the original research content as an appendix or reference section\n\n### Update screen functional specs\n- Add \"11. Rate App Screen\" section with: Route, Wireframe, States, Navigation, Invariants\n- **Fix quick-reference metadata counts** \u2014 update summary header to reflect actual screen count including Rate App\n\n### Update README\n- Add screen prompt link after Settings Screen entry (line 38)\n- Add/update spec link in Feature Specs section (line 65)\n\n## Acceptance\n- [ ] `docs/best_practices/design/screen-prompts/12-rate-app-screen.md` created with full screen prompt\n- [ ] `11-settings-screen.md` updated: Rate the App tile action updated, trailing icon corrected\n- [ ] Settings spec: placeholder removed from Tile Definitions, removed from Out of Scope, `in_app_review` added to Dependencies\n- [ ] `202603162012_spec_rate_app_integration.md` rewritten in standard spec format (Goal, Architecture, API Contracts, Edge Cases, Acceptance Criteria)\n- [ ] Screen functional specs updated with Rate App screen entry\n- [ ] Screen functional specs quick-reference metadata counts corrected\n- [ ] `docs/README.md` updated with screen prompt link and spec link\n- [ ] All doc formatting consistent with existing docs\n\n## Done summary\nTBD\n\n## Evidence\n- Commits:\n- Tests:\n- PRs:\n"
    }
  ]
}
