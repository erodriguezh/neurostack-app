# fn-45-f27.1 Update PaywallView and PaywallViewModel for RevenueCat integration (Phase 5.1-5.2)

## Description

From `plan_paywall_modal.md` Phase 5: Paywall Presentation.

### 5.1 Update PaywallView (CRITICAL - Navigation Semantics + Error Handling)

**File:** `lib/paywall/paywall_view.dart`

**Problem:** Current `navigateToHome()` uses `replaceAll`, breaking modal return flow.

**Fix:** Paywall must use `back()` when done (RouterService has no `pop(result)`).

**CRITICAL:** Show error UI on `PaywallOutcome.error` to prevent navigation loops.

**Changes:**
1. Present paywall via ViewModel on `initState`
2. Handle all `PaywallOutcome` cases:
   - `purchased`: navigate back
   - `cancelled`: navigate back
   - `error`: show snackbar, then navigate back
3. Use `locator<RouterService>().back()` instead of `replaceAll`

### 5.2 Update PaywallViewModel

**File:** `lib/paywall/paywall_view_model.dart`

**Changes:**
1. Constructor inject `RevenueCatService`
2. Add `presentPaywall()` method that delegates to service
3. Remove `navigateToHome()` - view handles navigation

## Implementation Notes

- Use root messenger to ensure snackbar survives route pop
- Brief delay (300ms) ensures snackbar renders before navigation
- Caller responsibility: HomeViewModel/LibraryViewModel should NOT auto-reopen paywall on error

## Acceptance

- [ ] PaywallViewModel receives RevenueCatService via constructor
- [ ] PaywallViewModel.presentPaywall() delegates to RevenueCatService
- [ ] PaywallView presents paywall on initState
- [ ] PaywallView uses back() for navigation after paywall closes
- [ ] PaywallView shows snackbar on error before navigating back
- [ ] `flutter analyze` passes
- [ ] Tests pass

## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
