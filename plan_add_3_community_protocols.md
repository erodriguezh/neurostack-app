# Plan: Add 3 Community-Validated Protocols (57 → 60)

- **Spec:** `docs/specs/20260506150000_spec_add_3_community_protocols.md`
- **Flow epic:** `fn-82-add-3-community-validated-protocols-to` — `.flow/specs/fn-82-add-3-community-validated-protocols-to.md`
- **Branch:** `fn-82-add-3-protocols`
- **Predecessor plan:** `plan_update_protocol_domain_and_db.md` (covers original 57-protocol seed + description column add)

---

## Goal

- Append 3 net-new community-validated protocols (Daily Calisthenics, Caffeine Delay, D3+K2) to the catalog.
- Pure data delta — **zero domain/DTO/schema/UI changes.**
- Catalog grows from 57 → 60 entries; citations 76 → 85.
- Premium users implicitly benefit (no per-protocol gating exists; gating is count-based at `lib/features/user/domain/enums/subscription_status.dart:4-22`).

---

## Approach (high-level)

- **Step 1** — Append 3 entries to `protocols.json` matching existing JSON shape exactly.
- **Step 2** — Regenerate seed migration via `tool/generate_seed_sql.dart` → new SQL file under `supabase/migrations/`.
- **Step 3** — Verify domain invariants pass for the 3 new entries via existing test suite.
- **Step 4** — Smoke-test idempotency by applying the migration twice locally.
- **Step 5** — Update doc count references.

---

## Phase 1 — Source data + migration *(maps to flow task `fn-82-add-3-community-validated-protocols-to.1`)*

### Files to modify

- `protocols.json` — append 3 entries inside the existing JSON array (do **not** reorder existing 57; do **not** add new top-level keys)

### Files to create

- `supabase/migrations/<UTC-timestamp>_seed_protocols_add3.sql` — generated by `tool/generate_seed_sql.dart`, do **not** hand-edit

### Step 1 — Append 3 entries to `protocols.json`

For each entry, mirror the exact key order of `protocols.json:1-50` (existing first entry as template):

- `name` (string, ≤100 chars, INV-P3 — `lib/features/protocol/domain/value_objects/protocol_name.dart:31-67`)
- `description` (string, non-empty trim, ≤2000 chars — `lib/features/protocol/domain/value_objects/protocol_description.dart:28-40`)
- `target.frequency.min_per_week` (int)
- `target.frequency.max_per_week` (int)
- `target.durationSeconds` (int)
- `target.intensity` (string)
- `category` (one of `exercise|heatTherapy|coldExposure|nutrition|supplements|mind|sleep` — `lib/features/protocol/domain/enums/category.dart:5-13`)
- `evidence_level` (one of `multipleRcts|singleRct|observational|expertConsensus` — `lib/features/protocol/domain/enums/evidence_level.dart:4-9`)
- `research_citations[]` (≥1 — INV-P1 — `lib/features/protocol/domain/entities/protocol.dart:59-72`)
- `source_models` (kept for parity — generator drops at `tool/generate_seed_sql.dart:145`)

#### Entry A — Daily Sub-Maximal Calisthenics for Functional Preservation

- `name`: "Daily Sub-Maximal Calisthenics for Functional Preservation"
- `category`: `exercise`
- `evidence_level`: `observational`
- `target.frequency`: 7 / 7 per week
- `target.durationSeconds`: 600
- `target.intensity`: "80–90% of max output, sub-failure, scale volume to capacity"
- `description`: mechanism + outcomes — daily neuromuscular maintenance via high-frequency low-volume bodyweight, scaling under failure as a behavioral feedback signal for systemic state; preserves lean mass, sustains systemic blood flow
- Citations:
  - Yang J et al., 2019, *JAMA Network Open*, doi `10.1001/jamanetworkopen.2018.8341`
  - Schoenfeld BJ, Grgic J, Krieger J, 2019, *J Sports Sciences*, doi `10.1080/02640414.2018.1555906`
  - Schoenfeld BJ, Ogborn D, Krieger J, 2017, *Sports Medicine*, doi `10.1007/s40279-016-0543-8`

#### Entry B — Delayed Morning Caffeine for Sustained Daytime Alertness

- `name`: "Delayed Morning Caffeine for Sustained Daytime Alertness"
- `category`: `mind`
- `evidence_level`: `expertConsensus`
- `target.frequency`: 7 / 7 per week
- `target.durationSeconds`: 5400 (delay window)
- `target.intensity`: "first caffeine 90–120 min after waking; water + electrolytes during delay"
- `description`: mechanism + outcomes — delaying first caffeine to allow cortisol awakening response to peak and adenosine to clear; eliminates afternoon crash, reduces caffeine dependence
- Citations:
  - Lovallo WR et al., 2005, *Psychosomatic Medicine*, doi `10.1097/01.psy.0000181270.20036.06`
  - Lovallo WR et al., 2006, *Pharmacol Biochem Behav*, doi `10.1016/j.pbb.2006.03.005`
  - Urry E, Landolt HP, 2014, *Curr Topics Behav Neurosci*, doi `10.1007/7854_2014_274`

#### Entry C — Vitamin D3 with K2 (MK-7) for Bone and Cardiovascular Health

- `name`: "Vitamin D3 with K2 (MK-7) for Bone and Cardiovascular Health"
- `category`: `supplements`
- `evidence_level`: `singleRct`
- `target.frequency`: 7 / 7 per week
- `target.durationSeconds`: 30 (compliance interval)
- `target.intensity`: "5,000 IU D3 + ~50 mcg K2 (MK-7) once daily"
- `description`: mechanism + outcomes — combined daily supplementation; K2 directs calcium to bones rather than arteries; addresses arterial-calcification concern when D3 is supplemented alone
- Citations:
  - Geleijnse JM et al., 2004, *J Nutrition*, doi `10.1093/jn/134.11.3100`
  - Knapen MHJ et al., 2013, *Osteoporosis Int*, doi `10.1007/s00198-013-2325-6`
  - Martineau AR et al., 2017, *BMJ*, doi `10.1136/bmj.i6583`

### Step 2 — Pre-generation safety checks

- Validate name lengths: `jq -r '.[].name | length' protocols.json | sort -nr | head -3` — top must be ≤100
- Validate INV-P3 patterns: `jq -r '.[].name' protocols.json | grep -iE "(s' |Dr\\. |The .+ (Protocol|Method|Approach))"` — must return zero new matches
- Validate dollar-quote collision: `grep -E '\$(n|desc|tgt|cat|ev)\$' protocols.json` — must return nothing inside string values
- Validate 60 entries: `jq 'length' protocols.json` → must equal 60
- Validate 85 citations: `jq '[.[].research_citations | length] | add' protocols.json` → must equal 85
- Validate categories: `jq -r '.[].category' protocols.json | sort -u` — must be subset of the 7 valid enum values
- Validate evidence levels: `jq -r '.[].evidence_level' protocols.json | sort -u` — must be subset of the 4 valid enum values

### Step 3 — Generate the migration

```bash
dart run tool/generate_seed_sql.dart > supabase/migrations/$(date -u +%Y%m%d%H%M%S)_seed_protocols_add3.sql
```

- Confirm the new file matches the existing exemplar shape (`supabase/migrations/20260221200000_seed_protocols.sql:15-27, 819-832`):
  - 60 protocol upsert blocks (each `INSERT INTO public.protocols ... ON CONFLICT (lower(name)) DO UPDATE SET ...`)
  - 85 citation rows (DELETE-then-INSERT per protocol)
  - SHA-256 audit comment at the bottom

### Step 4 — Domain invariant tests

```bash
flutter test test/domain/protocol/
flutter test test/features/protocol/data/dtos/
flutter analyze
```

- Existing tests:
  - `test/domain/protocol/protocol_test.dart` — entity invariants (INV-P1, immutability, soft-delete)
  - `test/features/protocol/data/dtos/protocol_dto_test.dart` — DTO `toDomain` / `fromJson` / roundtrip
- No test count assertions for protocols exist (verified — only mocked length-of-3 in `test/features/protocol/data/repositories/protocol_repository_impl_test.dart:181` which is unrelated)

### Step 5 — Idempotency smoke test

```bash
supabase db reset
supabase db push    # first apply
supabase db push    # second apply — must be no-op
```

- Both runs should leave `protocols` with 60 rows and `research_citations` with 85 rows.
- No `duplicate key value violates unique constraint "protocols_name_unique"` errors.

### Step 6 — DOI verification

- For each of the 9 new DOIs, verify resolution:
```bash
for doi in \
  10.1001/jamanetworkopen.2018.8341 \
  10.1080/02640414.2018.1555906 \
  10.1007/s40279-016-0543-8 \
  10.1097/01.psy.0000181270.20036.06 \
  10.1016/j.pbb.2006.03.005 \
  10.1007/7854_2014_274 \
  10.1093/jn/134.11.3100 \
  10.1007/s00198-013-2325-6 \
  10.1136/bmj.i6583; do
  curl -ILs "https://doi.org/$doi" -o /dev/null -w "%{http_code} $doi\n"
done
```

- Expect `30x` (redirect to publisher) for all 9. If any returns 404, swap for an alternate or escalate — do not ship placeholder DOIs.

---

## Phase 2 — Doc count updates *(maps to flow task `fn-82-add-3-community-validated-protocols-to.2`)*

### Files to modify

- `docs/specs/20260220120000_spec_protocol_description_and_seed.md`
  - Line 11: `57 science-backed protocols` → `60 science-backed protocols`
  - Line 14: `57 protocols` → `60 protocols`
  - Line 20: `57 protocols and 76 citations` → `60 protocols and 85 citations`
  - Line 28: `**57 protocols**, **76 citations** total` → `**60 protocols**, **85 citations** total`
  - Line 38: `57/57 protocols` → `60/60 protocols`
  - Line 46: `populated all 57` → leave (historical narrative; refers to past migration scope)
  - Lines 66–67: `57/57` coverage cells → `60/60`
  - Lines 68–69: `76/76` → `85/85`; `missing 2/76` → `missing 2/85` (3 new entries all ship with DOIs)

- `docs/README.md`
  - Line 68: `57 protocols, idempotent upsert` → `60 protocols, idempotent upsert`
  - Line 83: `seed 57 protocols` → `seed 60 protocols`
  - **Add new entries to the index** (under "Feature Specs" + "Implementation Plans"):
    - `Spec: Add 3 Community Protocols → docs/specs/20260506150000_spec_add_3_community_protocols.md`
    - `Plan: Add 3 Community Protocols → plan_add_3_community_protocols.md`

- `plan_update_protocol_domain_and_db.md`
  - Lines 78–81: `57 protocols + 76 citations` → `60 protocols + 85 citations`
  - Line 91: `57 protocols` → `60 protocols`

### Files NOT to modify (false-positive avoidance)

- `docs/ubiquitous-language.md` — no count references
- `CLAUDE.md` / `AGENTS.md` — no count references
- `test/features/protocol/data/dtos/research_citation_test.dart:184, 198` — `57` is a string-truncation length, unrelated
- `lib/paywall/widgets/trial_expired_modal.dart` tests — unrelated
- `docs/legal/research-findings.md` — `57` is a line-number reference, unrelated

### Verification

- Diff before/after: `git grep -nE '\b(57|76)\b' -- '*.md'` — ensure only the targeted lines changed
- `flutter test` and `flutter analyze` still pass (doc edits cannot affect, but green run is the gate)

---

## Acceptance (epic-level — see `.flow/specs/fn-82-add-3-community-validated-protocols-to.md` for R-IDs)

- [ ] **R1** — `protocols.json` has 60 entries; existing 57 byte-identical
- [ ] **R2** — 3 new entries fully shaped, ≥1 cited DOI each
- [ ] **R3** — Domain invariants pass for new entries (INV-P1, INV-P3, name ≤100, description ≤2000, valid enums)
- [ ] **R4** — New idempotent migration generated; double-apply produces no diff
- [ ] **R5** — Doc count references updated (57→60, 76→85)
- [ ] **R6** — `flutter test` passes unchanged

---

## Risks + Mitigations

- **DOI rot at CI time** — pre-verified at plan time; if any fails, swap alternate (do not placeholder)
- **Dollar-quote collision in description prose** — quick `grep` pre-check; existing 57 have not collided
- **Migration timestamp collision at merge** — rename with later UTC timestamp; never reorder applied migrations
- **Hard-coded count assertions** — verified none exist in repo; use `>= N` style if any are added later
- **Description over 2000 chars** — domain `ProtocolDescription.create` rejects; tested in Phase 1 Step 4
- **INV-P3 name regex match** — names vetted against `lib/features/protocol/domain/value_objects/protocol_name.dart:31-67`; re-vet if names are revised

---

## References

- **Spec:** `docs/specs/20260506150000_spec_add_3_community_protocols.md`
- **Source research:** `protocols2.md` (repo root)
- **Predecessor spec:** `docs/specs/20260220120000_spec_protocol_description_and_seed.md`
- **Generator pipeline:** `tool/generate_seed_sql.dart:18-38, 67-106, 108-165, 200-222`
- **Exemplar migration:** `supabase/migrations/20260221200000_seed_protocols.sql:9, 15-27, 819-832`
- **Domain constraints:** `lib/features/protocol/domain/value_objects/{protocol_name,protocol_description,target}.dart`
- **Premium gating:** `lib/features/user/domain/enums/subscription_status.dart:4-22`, `lib/features/user/domain/entities/user.dart:109`
- **Related flow epics (done):**
  - `fn-67-phase-32-seed-migration-upsert-57`
  - `fn-72-refactor-protocol-description-seed`
